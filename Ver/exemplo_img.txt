✅ Pronto!
Montei um **dataset sintético de exemplo** com **5 imagens anotadas no formato YOLO** para detecção de EPIs (capacete, óculos, colete).

Você pode baixar aqui:

📂 [dataset\_epi (ZIP)](sandbox:/mnt/data/dataset_epi)

Esse dataset contém:

* `images/train/` → 5 imagens de trabalhadores (formas geométricas simulando EPIs).
* `labels/train/` → arquivos `.txt` de anotações no formato YOLO.
* `data.yaml` → configuração do dataset (classes e caminhos).

---

Quer que eu também compacte tudo em **.zip** para você importar direto no **Roboflow** ou usar no treino do **YOLOv5/YOLOv8**?
